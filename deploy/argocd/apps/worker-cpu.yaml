apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: worker-cpu
spec:
  project: default
  source:
    repoURL: https://github.com/example/youtube-tool
    targetRevision: HEAD
    path: deploy/charts/worker-cpu
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
