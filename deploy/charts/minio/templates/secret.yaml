apiVersion: v1
kind: Secret
metadata:
  name: {{ include "minio.fullname" . }}-secret
type: Opaque
stringData:
  MINIO_ROOT_USER: {{ .Values.secret.rootUser | quote }}
  MINIO_ROOT_PASSWORD: {{ .Values.secret.rootPassword | quote }}
