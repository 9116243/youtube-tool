apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "worker-cpu.fullname" . }}-config
  labels:
    {{ include "worker-cpu.labels" . | indent 4 }}
data:
{{- if .Values.env }}
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ quote $value }}
{{- end }}
{{- else }}
  NODE_ENV: production
{{- end }}
